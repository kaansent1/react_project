version: '3.8'
services:
  postgresql:
    image: postgres
    environment:
      POSTGRES_USER: myusername
      POSTGRES_PASSWORD: password
    ports:
      - "8000:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
